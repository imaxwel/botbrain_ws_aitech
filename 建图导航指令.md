# 建图与导航快速指令

## 现场常用

> 将 `scene=floor16_20260730` 改为本次地图名，将 `192.168.37.204` 改为机器人 Wi-Fi IP。同一个 `scene` 必须用于建图、保存、定位和点位。

### 任务一：建图

机器人端启动建图，每次使用一个新的 `scene`：

```bash
cd /data/unitree/botbrain_ws
scene=floor16_20260730我主要研究具身智能和机器人算法工程，比较熟悉从感知、定位、规划控制到策略部署的完整链路。  
在实习中，我参与了 G1 机器人的导航、Dex3 灵巧手视觉操作以及仿真到真实部署。导航部分基于 FAST-LIO2、ROS2 Nav2 和轨迹规划，实现了建图定位、路径规划、障碍物避障以及多目标点连续导航。操作部分使用 D435i、AprilTag、YOLO-NAS 和 RGB-D 信息进行目标定位，再结合 TRAC-IK 和 OMPL 完成机械臂到目标点的运动规划和按钮按压。  
另外，我参与过 OpenVLA、Pi0、GR00T 等视觉语言动作模型的微调、推理和 LIBERO 仿真评测，做过 LoRA/QLoRA、Flow Matching 推理链路以及模型部署优化。  
我比较关注算法在真实机器人上的落地，包括控制频率、传感器延迟、坐标系、碰撞安全和 Sim2Real 误差。希望在贵公司的岗位中，进一步深入运动控制、全身移动和强化学习策略训练，把实验结果做成稳定可复现的机器人系统。我主要研究具身智能和机器人算法工程，比较熟悉从感知、定位、规划控制到策略部署的完整链路。  
在实习中，我参与了 G1 机器人的导航、Dex3 灵巧手视觉操作以及仿真到真实部署。导航部分基于 FAST-LIO2、ROS2 Nav2 和轨迹规划，实现了建图定位、路径规划、障碍物避障以及多目标点连续导航。操作部分使用 D435i、AprilTag、YOLO-NAS 和 RGB-D 信息进行目标定位，再结合 TRAC-IK 和 OMPL 完成机械臂到目标点的运动规划和按钮按压。  
另外，我参与过 OpenVLA、Pi0、GR00T 等视觉语言动作模型的微调、推理和 LIBERO 仿真评测，做过 LoRA/QLoRA、Flow Matching 推理链路以及模型部署优化。  
我比较关注算法在真实机器人上的落地，包括控制频率、传感器延迟、坐标系、碰撞安全和 Sim2Real 误差。希望在贵公司的岗位中，进一步深入运动控制、全身移动和强化学习策略训练，把实验结果做成稳定可复现的机器人系统。
docker compose stop localization navigation
docker compose up -d zenoh bringup state_machine
touch "botbrain_ws/src/g1_pkg/maps/.${scene}_mapping_started"
FAST_LIO_START_DELAY_SEC=0 FAST_LIO_MAPPING_MODE=true FAST_LIO_MAPPING_SAVE=true \
FAST_LIO_MAP_FILE="/botbrain_ws/src/g1_pkg/maps/${scene}_fast_lio_raw.pcd" \
FAST_LIO_MAPPING_PROFILE=default FAST_LIO_LOOP_POSE_GRAPH=true \
FAST_LIO_LOOP_LIVE_CORRECTION=false \
docker compose up -d --force-recreate fast_lio
docker compose logs -f fast_lio
```

工作站观察建图：

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/mapping_rviz2.sh 192.168.37.204
```

看到 `IMU Initial Done`、连续 `FAST_LIO_TIMING ok=true`，且红色世界点云和栅格正常后再移动。完成后在机器人端保存并停止建图：

```bash
docker exec -it g1_robot_fast_lio \
  bash /botbrain_ws/tools/mapping/mapping_save.sh "$scene"
docker compose stop -t 180 fast_lio
```

### 任务二：定位与导航

机器人端启动定位：

```bash
cd /data/unitree/botbrain_ws
scene=floor16_20260730
docker compose stop navigation
docker compose up -d zenoh bringup state_machine
FAST_LIO_START_DELAY_SEC=0 FAST_LIO_MAPPING_PROFILE=default \
FAST_LIO_MAPPING_MODE=false FAST_LIO_MAPPING_SAVE=false FAST_LIO_MAP_FILE= \
docker compose up -d --force-recreate fast_lio
MAP_SCENE="$scene" LOCALIZATION_START_DELAY_SEC=0 \
LOCALIZATION_MODE=cold_start \
LOCALIZATION_INITIAL_POSE_PRIORS= LOCALIZATION_INITIAL_POSE_PRIOR_NAMES= \
docker compose --profile navigation up -d --force-recreate --no-deps localization
docker compose logs -f localization
```

工作站打开导航 RViz2：

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/g1_nav_loc_rviz2.sh 192.168.37.204
```

深蓝色 `/pcd_map` 和红色 `/scan2map` 必须可见。自动定位失败时，用 `2D Pose Estimate` 在实际位置点击并拖出朝向。日志出现 `Localization ready` 后，在机器人端启动 Navigation：

```bash
docker compose --profile navigation up -d --force-recreate navigation
docker compose logs -f navigation
```

看到 `Navigation preflight passed` 后才能发送导航目标。

### 任务三：点位记录与导航

进入 Navigation 容器，所有命令都显式使用当前地图场景：

```bash
docker exec -it g1_robot_navigation bash
source /opt/ros/humble/setup.bash
source /botbrain_ws/install/setup.bash
scene=floor16_20260730

ros2 run bot_navigation waypoint_recorder.py record <name> --scene "$scene"
ros2 run bot_navigation waypoint_recorder.py list --scene "$scene"
ros2 run bot_navigation waypoint_recorder.py delete <name> --scene "$scene"
ros2 run bot_navigation waypoint_navigator.py <name> --scene "$scene"
ros2 run bot_navigation waypoint_navigator.py <p1> <p2> <p3> --scene "$scene"
ros2 run bot_navigation waypoint_navigator.py <p1> <p2> <p3> --scene "$scene" --loop
```

---

## 详细说明与排障

> 地图目录：`/data/unitree/botbrain_ws/botbrain_ws/src/g1_pkg/maps/`，以下以 `floor1` 为例。
>
> 可视化统一为工作站本地运行的 **RViz2 + Zenoh**。建图与导航均不使用 Foxglove；机器人和工作站必须能访问 Zenoh TCP 端口 `7448`。完整界面操作见同目录 [rviz-guide.md](rviz-guide.md)。
>
> 必须从项目内的两份预设启动 RViz2：建图使用 `configs/g1_mapping_rviz2.rviz`，定位/导航使用 `configs/g1_nav_loc_rviz2.rviz`；预设已经配置正确的 Fixed Frame、话题和交互工具。
>
> 这是可视化迁移，不是修复 Foxglove 的 ROS 数据问题：FAST-LIO、定位、导航、话题和 TF 链路保持原样，RViz2 直接订阅同一套 ROS 2 数据。
>
> 机器人 Wi‑Fi IP 变化时，只替换 RViz 启动脚本最后的 IP 参数，例如建图使用 `bash tools/host_side/mapping_rviz2.sh 192.168.100.3`，导航使用 `bash tools/host_side/g1_nav_loc_rviz2.sh 192.168.100.3`；不要修改 Unitree 内部 `192.168.123.x` 控制网。
>
> 若需要进行更换wifi，需要sudo nmcli connection up G1-MAP

---

## 一、建图

### 步骤 0：停止定位与导航

```bash
cd /data/unitree/botbrain_ws
docker compose stop localization navigation
```

建图期间保持这两个服务停止，避免旧地图和定位 TF 与新地图叠加。无需手动 `rm` 容器。

### 步骤 1：首次部署代码后编译

只有代码或 launch 有更新时才编译；日常建图不再修改 `mid360.yaml`：

```bash
cd /data/unitree/botbrain_ws
docker compose up builder_base
```

### 步骤 2：直接启动建图服务

直接启动服务；每次使用从未用过的新场景名，不要复用
`floor13/floor14/floor15`：

```bash
cd /data/unitree/botbrain_ws
scene=floor16_20260728  # 必须改成一个从未使用过的新名字

docker compose stop localization navigation
docker compose up -d zenoh bringup state_machine
touch "botbrain_ws/src/g1_pkg/maps/.${scene}_mapping_started"
FAST_LIO_START_DELAY_SEC=0 \
FAST_LIO_MAPPING_MODE=true \
FAST_LIO_MAPPING_SAVE=true \
FAST_LIO_MAP_FILE="/botbrain_ws/src/g1_pkg/maps/${scene}_fast_lio_raw.pcd" \
FAST_LIO_MAPPING_PROFILE=default \
FAST_LIO_LOOP_POSE_GRAPH=true \
FAST_LIO_LOOP_LIVE_CORRECTION=false \
docker compose up -d --force-recreate fast_lio

# 立即查看容器状态和完整 FAST-LIO 日志
docker compose ps zenoh bringup state_machine fast_lio
docker compose logs -f fast_lio
```

启动前只需换一个新名字。若这个名字已经用过，就重新设置变量再启动：

```bash
scene=floor16_v2
# 然后重新执行上面的 FAST_LIO_MAPPING_* + docker compose up 命令
```

看到 `IMU Initial Done`、连续 `[FAST_LIO_TIMING] ok=true`，并在
RViz2 看到世界点云和累计栅格后才能移动。`Ctrl+C` 只退出 `logs -f`，后台服务继续运行。

以下检查仅在 RViz 没有实时数据时执行：

```bash
docker exec -it g1_robot_fast_lio bash
source /opt/ros/humble/setup.bash && source /botbrain_ws/install/setup.bash
timeout 10 ros2 topic hz /livox/imu || true
timeout 10 ros2 topic hz /cloud_registered_1 || true
timeout 10 ros2 topic hz /cloud_registered_body_1 || true
timeout 10 ros2 topic hz /Odometry_loc || true
timeout 10 ros2 topic hz /accumulated_grid || true
```

### 步骤 2.1：工作站打开 RViz2 观察建图

> 在 **workstation** 执行。工作站只需安装一次 `ros-humble-rmw-zenoh-cpp` 和 `ros-humble-rviz2`；脚本会自动连接 Zenoh、刷新 ROS 2 图并打开建图预设。

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/mapping_rviz2.sh 192.168.37.204  # 改为当前 G1 IP
```

RViz2 建图预设的 `Fixed Frame` 已是 `camera_init`，用于直接判断原始 FAST-LIO 的逐帧建图是否稳定；默认不显示在线回环 TF。`/cloud_registered_1`
预置了两个无需 Add 的显示层：明亮的 `world scan (live)` 保证当前帧立即
可见，`world history (2 min)` 保留最近 120 秒用于观察建图轮廓；白色
`/cloud_registered_body_1`、累计栅格、里程计、轨迹和 TF 也均已预置。
两个世界点云层均为红色，Body 点云为白色。不要用 Body 点云判断世界地图漂移。

如果工作站为 Intel/Mesa，并出现
`active samplers with a different type refer to the same texture image unit`，
使用软件渲染回退重新打开（仅在出现该 GLSL 错误时使用）：

```bash
RVIZ_RENDERING=software \
bash tools/host_side/mapping_rviz2.sh 192.168.37.204
```

长走廊与普通区域使用同一套固定参数；不需要也不能通过选择 `corridor` 改变 FAST-LIO 前端。旧 `corridor` 参数只作兼容输入，会被归一为 `default`。这样不会再裁掉可能是走廊纵向唯一约束的远端墙、门框和拐角。回环位姿图仍只在连续 3 个相对第一个一致的高置信候选后优化，但默认不发布 `map→camera_init`；单次候选不会改原始图或 TF，也不会替代 FAST-LIO 的逐帧局部估计。经过开门房间时应减速或短暂停留，并左右转动观察房间角点；走廊尽头不要原地一次性转过 180 度，分段转向并确认 RViz 中门框、房间轮廓没有出现双层后再返回。

栅格地面平面拟合是 fail-closed：RANSAC 无有效 `candidate` 或出现 `plane_jump` 时，当帧不分类、不写 free/occupied，最后一个有效平面只保留作显示。稳定高度变化必须有连续 3 个互相一致的新平面才会原子替换；中间任一候选不一致就从 `1/3` 重新确认。

### 步骤 3：保存地图

保存 2D 栅格：

```bash
docker exec -it g1_robot_fast_lio bash
source /opt/ros/humble/setup.bash && source /botbrain_ws/install/setup.bash
ros2 run nav2_map_server map_saver_cli \
  -t /accumulated_grid --free 0.196 --occ 0.65 \
  -f /botbrain_ws/src/g1_pkg/maps/floor1
```

主动保存 PCD：

```bash
ros2 service call /map_save std_srvs/srv/Trigger '{}'
cp -a /botbrain_ws/src/g1_pkg/maps/floor1_fast_lio_raw.pcd \
      /botbrain_ws/src/g1_pkg/maps/floor1_scans.pcd
exit
```

必须看到 `success=true` 且消息包含 `saved <点数> points to <目标文件>` 后才复制；否则不要停止 FAST-LIO，先检查 `/fast_lio` 的 `pcd_save.pcd_save_en=true` 和 `map_file_path` 是否为当前 `floor1_fast_lio_raw.pcd`。导航固定加载 `floor1_scans.pcd + floor1.yaml + floor1.pgm` 这一组原始同坐标成品。回环逐关键帧优化后的 PCD 只能另存为 `floor1_loop_optimized.pcd` 诊断，不能替换 `floor1_scans.pcd`，否则会与二维栅格错位。

停止 FAST-LIO：

```bash
cd /data/unitree/botbrain_ws
docker compose stop -t 180 fast_lio
```

不需要再把 `mid360.yaml` 改回去；建图开关只存在于本次 `fast_lio` 容器环境。之后通过场景选择器重建 FAST-LIO 时会回到导航安全默认值 `pcd_save_en=false`。

验证文件：

```bash
maps=/data/unitree/botbrain_ws/botbrain_ws/src/g1_pkg/maps
ls -lh "$maps/floor1_scans.pcd" "$maps/floor1.pgm" "$maps/floor1.yaml"
head -n 11 "$maps/floor1_scans.pcd" | grep -E "^(FIELDS|POINTS|DATA)"
```

### 步骤 4：PCD 高度校正（只做一次）

```bash
cd /data/unitree/botbrain_ws
maps=botbrain_ws/src/g1_pkg/maps
test ! -e "$maps/floor1_scans_raw.pcd" || {
  echo "ERROR: floor1_scans_raw.pcd 已存在，禁止覆盖或重复高度校正"; exit 1;
}
cp -a "$maps/floor1_scans.pcd" "$maps/floor1_scans_raw.pcd"

docker compose run --rm builder_base bash -lc '
  source /opt/ros/humble/setup.bash && source /botbrain_ws/install/setup.bash
  python3 /botbrain_ws/install/g1_pkg/lib/g1_pkg/shift_pcd_z.py \
    /botbrain_ws/src/g1_pkg/maps/floor1_scans.pcd 1.247
'
```

校正后不要再次以建图模式写入该成品 PCD；进入定位/导航时使用后文场景选择器重建 FAST-LIO。

### 步骤 5：确认成品地图场景

```bash
scene=floor1
maps=/data/unitree/botbrain_ws/botbrain_ws/src/g1_pkg/maps
test -s "$maps/${scene}_scans.pcd" \
  && test -s "$maps/${scene}.yaml" \
  && test -s "$maps/${scene}.pgm" || {
    echo "ERROR: $scene 的地图三文件缺失或为空"; exit 1;
  }
sed -n '1,10p' "$maps/${scene}.yaml"
```

标准命名固定为 `<scene>_scans.pcd + <scene>.yaml + <scene>.pgm`，YAML 的 `image` 必须指向同场景 PGM。日常通过重建 localization 服务时传入 `MAP_SCENE="$scene"` 选择整套地图，不再创建 `scans.pcd`、`accumulated.yaml` 软链接。单独 `export MAP_SCENE=...` 不会切换已运行容器，必须执行后文的 `docker compose ... --force-recreate ... localization`。完整三文件检查见 `机器人项目run.md` 的 2.3。

### 步骤 6：建图完成后查看效果

直接启动 `bringup + fast_lio + localization` 三个 Compose 服务，但不启动 `navigation`：

```bash
cd /data/unitree/botbrain_ws
scene=floor1

docker compose up -d zenoh bringup state_machine
docker compose stop navigation

FAST_LIO_START_DELAY_SEC=0 FAST_LIO_MAPPING_PROFILE=default \
FAST_LIO_MAPPING_MODE=false FAST_LIO_MAPPING_SAVE=false FAST_LIO_MAP_FILE= \
docker compose up -d --force-recreate fast_lio

MAP_SCENE="$scene" LOCALIZATION_START_DELAY_SEC=0 \
LOCALIZATION_MODE=cold_start \
LOCALIZATION_INITIAL_POSE_PRIORS= LOCALIZATION_INITIAL_POSE_PRIOR_NAMES= \
docker compose --profile navigation up -d --force-recreate --no-deps localization

docker compose ps fast_lio localization
docker compose logs -f localization | \
  grep --line-buffered -E "Map selection:|Localization ready|ICP: accepted|ERROR|FATAL"
```

按 `Ctrl+C` 只退出日志跟随，不会停止后台服务。如果需要 ICP/TF/发布者的自动严格验收，可另行执行保留的 `select_map_scene.sh`，但它不是本步骤的启动入口，也不是日常启动服务的必经步骤。

在 workstation 执行下面一条命令打开导航预设：

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/g1_nav_loc_rviz2.sh 192.168.37.204
```

- `/localization_ready=false` 时就打开导航预设（Fixed Frame=`map`）：深蓝色 `/pcd_map` 与红色 `/scan2map` 必须可见，可直接用 `2D Pose Estimate` 辅助匹配。若要单独诊断 FAST-LIO 原始世界云、Body 云和 `/path_1`，再另开建图预设（Fixed Frame=`camera_init`）。
- `/map` 出现不等于定位完成；导航预设可以提前保持打开，但只有日志出现 `Localization ready` 或 `/localization_ready=true` 后才允许启动 Navigation。
- 导航预设的 Fixed Frame 已设为 `map`，自动包含 `/map`、`/pcd_map`、`/cloud_registered_1`、`/cloud_registered_body_1`、`/scan`、`/g1_robot/plan`、TF、可选 global/local costmap 与 `/g1_robot/goal_pose` 工具，无需手动 Add。
- 修改过预设后先关闭旧 RViz2 再重开；若左侧仍出现 `RobotModel (G1)`，说明当前是旧窗口，当前预设没有 RobotModel。
- 俯视检查 2D 地图与 PCD 墙体轮廓，侧视检查 PCD 高度、倾斜和实时点云是否重合。
- ready 前以 `map` 为 Fixed Frame 时，红色世界云、白色 Body 云、Path 和 Scan 可能因缺少 `map -> odom` TF 而显示 Warm；深蓝色 `/pcd_map` 与红色 `/scan2map` 仍必须可见，这两层正是发送 `2D Pose Estimate` 的依据。
- 导航规划禁止进入未知区：`allow_unknown=false`，global costmap 必须保留 unknown。地图边界存在漏口时用 Map Editor 补虚拟墙；不要让规划器把灰色未知区当捷径。路径代价权重固定为 `3.0`，避免近距离目标被软代价推成几十米绕路。
- 行为树只在目标更新或持续控制失败后重做全局路径，不再根据瞬时 `/scan` 每两秒切换路径。MPPI 前进上限为 `0.50m/s`；`velocity_smoother` 将 `/g1_robot/cmd_vel_nav_raw` 以 20 Hz 平滑为 `/g1_robot/cmd_vel_nav`，减少单个控制周期延迟造成的“走几步停一下”。

机器人静止、直行和左右转弯后，`/cloud_registered_1` 都应与 `/pcd_map` 和 `/map` 保持重合。

2D 孤立黑点可进入下一节修图；如果 PCD 已经出现双墙、倾斜或明显漂移，不能只修 PGM，应重新检查或重新建图。

---

## 二、地图编辑（Map Editor）

> workstation 端操作，不能在 G1 宿主机上运行。

### 一次性准备

```bash
cd /home/aitech/Workspace/botbrain_project
docker build -t map_edit_rviz:latest tools/host_side/map_edit
docker rm -f map_edit_rviz 2>/dev/null || true
docker run -d --name map_edit_rviz \
    -e DISPLAY="$DISPLAY" \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v "$PWD/botbrain_ws/src/g1_pkg/maps":/root/maps \
    map_edit_rviz:latest

docker inspect map_edit_rviz --format \
  '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'
```

### 编辑流程

```bash
# 1. 从 G1 拉取地图
scp unitree@<G1_IP>:/data/unitree/botbrain_ws/botbrain_ws/src/g1_pkg/maps/floor1.{pgm,yaml} \
    botbrain_ws/src/g1_pkg/maps/

# 2. 启动编辑器（脚本会先校验容器挂载和地图文件）
bash tools/host_side/map_edit/start_map_edit.sh floor1.yaml

# 3. 编辑完成后在 RViz 中点击保存，并检查 YAML
# 编辑完成后在 RViz 中点击 Save All Files
sed -n '1,10p' botbrain_ws/src/g1_pkg/maps/floor1.yaml

# 4. 推回 G1
scp botbrain_ws/src/g1_pkg/maps/floor1.{pgm,yaml} \
    "unitree@<G1_IP>":/data/unitree/botbrain_ws/botbrain_ws/src/g1_pkg/maps/
```

若出现 `Map_server could not open ...` 或 `YAML parse error: bad file`，表示容器内打不开该路径，通常是容器仍挂载旧仓库目录，不是 `mode: trinary` 导致。删除并按“一次性准备”重建 `map_edit_rviz`；不要在空白界面保存地图。

---

## 三、定位与导航启动

### 启动前编译

```bash
cd /data/unitree/botbrain_ws
docker compose up builder_base
```

### 启动定位

```bash
cd /data/unitree/botbrain_ws
scene=floor1  # 改为机器人当前所在场景，例如 ug、floor4
docker compose stop navigation
docker compose up -d zenoh bringup state_machine

# 冷启动、刚结束建图、跨楼层或 FAST-LIO 不健康时执行；
# 同楼层且 FAST-LIO 已健康时可跳过这一段。
FAST_LIO_START_DELAY_SEC=0 FAST_LIO_MAPPING_PROFILE=default \
FAST_LIO_MAPPING_MODE=false FAST_LIO_MAPPING_SAVE=false FAST_LIO_MAP_FILE= \
docker compose up -d --force-recreate fast_lio

# 直接启动当前场景的 localization 服务。
MAP_SCENE="$scene" LOCALIZATION_START_DELAY_SEC=0 \
LOCALIZATION_MODE=cold_start \
LOCALIZATION_INITIAL_POSE_PRIORS= LOCALIZATION_INITIAL_POSE_PRIOR_NAMES= \
docker compose --profile navigation up -d --force-recreate --no-deps localization

docker compose ps fast_lio localization
docker compose logs -f localization
```

上述 `up -d` 命令创建容器后立即返回；FAST-LIO 和 localization 在后台初始化。按 `Ctrl+C` 只退出日志跟随，不会停止服务。此阶段必须保持 Navigation 停止。

在工作站直接打开 RViz2：

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/g1_nav_loc_rviz2.sh 192.168.37.204
```

- 定位与导航共用这一个 RViz2 窗口，Fixed Frame 必须始终为 `map`；只有建图预设才使用 `camera_init`
- 深蓝色 `/pcd_map`、红色 `/scan2map`、红色世界点云和白色 Body 点云都已预置，不需要手动 Add
- 先等待 FPFH/RANSAC 自动初始化；只有持续失败时才在 RViz2 工具栏选择 `2D Pose Estimate` 发送 `/initialpose`，并确认 `Manual relocalization applied`
- localization 日志必须显示当前 `Map selection: scene=<scene>`、`Localization ready`，且 `/localization_ready=true`、点云与地图重合；否则不能启动导航
- `ready=false confidence=0.000 tf=false` 表示定位未完成。若 60～90 秒仍无法自动匹配，使用 `2D Pose Estimate`，不要启动 Navigation 来等定位
- 如果深蓝色 `/pcd_map` 也没有，先检查 `docker compose ps localization` 和 `docker compose logs localization`，不能靠 RViz 手动 Add 补出数据
- 若提示 `navigation install space is missing or stale`，先执行本节“启动前编译”，不能继续使用旧 install 产物

需要自动检查 ICP、TF、Unitree twist 和发布者唯一性时，才可选执行保留的严格验收脚本：

```bash
bash tools/nav/select_map_scene.sh "$scene" --wait-ready --ready-timeout 300
# 冷启动或跨楼层验收时再加 --restart-fast-lio
```

### 启动导航

只有 localization 日志出现 `Localization ready`、`/localization_ready=true`且 RViz 中实时点云与地图重合时才执行。如果使用了可选严格验收脚本，则以脚本返回 `0` 为准；仍在打印 `Waiting for navigation inputs` 就表示不能启动。

```bash
cd /data/unitree/botbrain_ws
docker compose --profile navigation up -d --force-recreate navigation
docker compose logs -f navigation
```

Navigation 启动后会立即执行独立 preflight。定位已成功时通常数秒内进入 Nav2；若定位未通过，preflight 最多等待 60 秒后退出，绝不会用未验证的 `map` TF 开始运动。

发送目标前还必须确认 `docker compose logs --tail 50 bringup` 中没有持续出现：

```text
locomotion Move failed: code=3104
get_balance_mode rc=7301
```

Unitree SDK 对这两个码的定义分别是 `Call api timeout error` 和
`LocoState not available`。这是机器人本体 locomotion 服务没有响应，不是 RViz、
Zenoh 或 Nav2 可视化问题；即使 `192.168.123.161` 能 ping 通，也不能在该状态下
验收导航运动。先恢复机器人本体运动服务/模式，再发送 `2D Goal Pose`。

看到 `Managed nodes are active` 且没有 `ERROR` 后按 `Ctrl+C` 退出日志跟随；后台 Navigation 不会停止。详细 lifecycle 检查仅在启动失败时参考 `机器人项目run.md`。

如果定位阶段还没有打开 RViz2，在工作站执行；如果已打开，直接继续使用同一个窗口：

```bash
cd ~/Workspace/botbrain_project
bash tools/host_side/g1_nav_loc_rviz2.sh 192.168.37.204
```

确认 Fixed Frame=`map`、`Map`=`/map`、深蓝色静态点云=`/pcd_map`、红色世界点云=`/cloud_registered_1`、白色 Body 点云=`/cloud_registered_body_1`、红色候选点云=`/scan2map`、实时激光=`/scan`。这些均由预设自动加载，不需要 Add。仅当 `Localization ready`、Nav2 生命周期检查均通过后，才在工具栏选择 `2D Goal Pose` 并在地图上点击拖拽目标朝向；预设会向 `/g1_robot/goal_pose` 发布目标。需要人工重定位时选择 `2D Pose Estimate`，同样在 Fixed Frame=`map` 下点击拖拽；不要在 `camera_init` 下发送 `/initialpose`。RViz 图层和视角调整见 `rviz-guide.md`，现场不需要手动执行 `source`、`ros2 daemon` 或 `rviz2 -d` 命令。

### 点位记录与导航

```bash
docker exec -it g1_robot_navigation bash
source /opt/ros/humble/setup.bash && source /botbrain_ws/install/setup.bash
scene=ug# 改成当前 localization 加载的地图场景。
ros2 run bot_navigation waypoint_recorder.py record ug_face_evelator --scene "$scene"			# 记录点位
ros2 run bot_navigation waypoint_recorder.py list --scene "$scene"								# 查看当前场景的点位
ros2 run bot_navigation waypoint_recorder.py delete ug_old --scene "$scene"						# 删除指定点位，然后再次查看确认
ros2 run bot_navigation waypoint_navigator.py aitech_v2_1 --scene "$scene"					# 单点导航
ros2 run bot_navigation waypoint_navigator.py aitech_v2_1 aitech_v2_2 aitech_v2_3 --scene "$scene"		# 多点导航
ros2 run bot_navigation waypoint_navigator.py aitech_v2_1 aitech_v2_2 aitech_v2_3 --scene "$scene" --loop  # 多点循环导航（可选）
```

### 语音到点位的匹配

语音导航只允许使用当前场景在 `nav_waypoints.yaml` 中已经登记的点位。当前仓库 `ug` 场景的匹配为：

| 语音说法 | 实际点位名 | 等价导航命令 |
| --- | --- | --- |
| 家、回家 | `home` | `ros2 run bot_navigation waypoint_navigator.py home --scene ug` |
| 办公室一/1 | `office1` | `ros2 run bot_navigation waypoint_navigator.py office1 --scene ug` |
| 办公室二/2 | `office2` | `ros2 run bot_navigation waypoint_navigator.py office2 --scene ug` |
| 办公室三/3 | `office3` | `ros2 run bot_navigation waypoint_navigator.py office3 --scene ug` |
| 办公室四/4 | `office4` | `ros2 run bot_navigation waypoint_navigator.py office4 --scene ug` |
| 办公室五/5 | `office5` | `ros2 run bot_navigation waypoint_navigator.py office5 --scene ug` |
| 转弯点、转弯处 | `turn` | `ros2 run bot_navigation waypoint_navigator.py turn --scene ug` |

语音链路的数据流是：G1 本体 ASR 的最终文本 `/audio_msg` → 云端 LLM 返回白名单名称 → 本地别名解析为上表点位 → `/g1_robot/navigate_to_waypoint` → 现有 `waypoint_navigator.py` → `/g1_robot/navigate_to_pose`。当前文件没有 `ug_face_evelator`，所以“电梯”不会被发送；现场用 `waypoint_recorder.py record <name> --scene ug` 记录该点位后，才可新增对应语音别名。

### 启动语音导航服务

确认当前场景的定位和 Navigation 已就绪后，在机器人端执行：

```bash
cd /data/unitree/botbrain_ws
scene=ug  # 必须与 localization/navigation 当前加载的场景一致
MAP_SCENE="$scene" docker compose --profile voice_navigation up -d --force-recreate voice_navigation
docker compose logs -f voice_navigation
```

启动日志应显示监听 `/audio_msg, /rt/audio_msg`。云端 LLM/TTS 参数统一填写在项目的 `botbrain_ws/robot_config.yaml` 中，配置段为 `robot_configuration.voice_navigation`；其中 `llm_endpoint`、`llm_api_key`、`llm_model` 三项必填，缺少任一项时节点不会发送导航目标。完整的分阶段测试、干跑命令和待办见 `docs/voice-navigation-test-plan.md`。公开 Unitree Python SDK 的 `AudioClient.Init()` 不是 API 1002 的启动调用，因此本服务复用机器人系统已有的原生 ASR 发布者；若两个话题都没有消息，需要在机器人系统侧确认原生语音进程是否运行。

> 停止服务:docker compose stop navigation localization fast_lio
> `delete` 只删除名称完全一致的一个点位；名称不存在时会报错，不会修改文件。删除后无需重新编译或重启 Navigation，下一次执行 `waypoint_navigator.py` 时会重新读取文件。
>
> 点位文件：`/data/unitree/botbrain_ws/botbrain_ws/src/bot_navigation/nav_waypoints.yaml`。文件内按场景分区；直接服务流程中为记录、查看、删除和点位导航命令显式传入 `--scene "$scene"`。程序会再核对 localization 实际场景，并拒绝发送场景不一致或明显落在占用栅格上的目标。
>
> 首次部署先备份该文件，不要用仓库默认文件覆盖现场点位。旧顶层 `waypoints:` 格式会在选中正确场景后，于首次导航或 `record`/`delete` 时自动迁移到该场景。

---

## 四、多楼层地图切换

以下把目标场景写成 `floor4`。机器人必须先到达目标楼层、取消活动导航目标并停稳；禁止运动中热切图。日常切层仍直接重建 Compose 服务：

```bash
cd /data/unitree/botbrain_ws
scene=floor4

# 先按步骤 5 检查 floor4_scans.pcd、floor4.yaml、floor4.pgm
docker compose stop localization navigation
docker compose up -d zenoh bringup state_machine

# 在目标层重建 camera_init/odom 基准，并确保退出建图模式。
FAST_LIO_START_DELAY_SEC=0 FAST_LIO_MAPPING_PROFILE=default \
FAST_LIO_MAPPING_MODE=false FAST_LIO_MAPPING_SAVE=false FAST_LIO_MAP_FILE= \
docker compose up -d --force-recreate fast_lio

MAP_SCENE="$scene" LOCALIZATION_START_DELAY_SEC=0 \
LOCALIZATION_MODE=floor_transition \
LOCALIZATION_INITIAL_POSE_PRIORS= LOCALIZATION_INITIAL_POSE_PRIOR_NAMES= \
docker compose --profile navigation up -d --force-recreate --no-deps localization

docker compose ps fast_lio localization
docker compose logs -f localization | \
  grep --line-buffered -E "Map selection:|Global registration|Global candidate|Localization ready|ICP: accepted|ERROR|FATAL"

# 看到 Localization ready、/localization_ready=true 且点云重合后，最后启动导航。
docker compose --profile navigation up -d --force-recreate navigation
```

这里没有真实出口先验时会使用 `floor_transition` 的 BBS/全局匹配兜底；不会要求手工填写空的 launch 参数。需要自动校验三文件、IMU、ICP、TF 和发布者唯一性时，可选执行 `bash tools/nav/select_map_scene.sh floor4 --restart-fast-lio --wait-ready --ready-timeout 300`。原脚本保留不变，但不是日常切层入口。不要只调用 `map_server/load_map`，也不要用 `docker compose restart localization` 切层；后者不会带入新的 `MAP_SCENE`。

该流程支持每层独立导航和停车切层，不支持 Nav2 直接规划跨楼层连续路径，也不会自动识别楼层。跨层任务需由上层状态机拆成“到电梯 -> 乘梯 -> 目标层重建 FAST-LIO/定位 -> 使用目标层点位”。
